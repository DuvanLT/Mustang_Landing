'use client'
const CustomButton = ({title, styles}) => {
    return(
        <button
        type="button"
        className={styles}
        onClick={() => {}}
        >
            {title}
        </button>
    )
}

export default CustomButton